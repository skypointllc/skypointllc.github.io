# Family portrait assets

Keep family portrait sessions separate from event coverage. Add the upcoming father-and-daughters
session to `session-01/`; additional sessions can be added as `session-02/`, `session-03/`, and so on.

